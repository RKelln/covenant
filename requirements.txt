# Dependencies are managed via pyproject.toml.
# To install: uv sync
#
# Pinned lockfile is uv.lock (generated by uv sync).
# For environments without uv, run: pip install pyyaml jsonschema weasyprint markdown
