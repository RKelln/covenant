{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "assembly.schema.json",
  "title": "Covenant Assembly Manifest",
  "description": "Schema for assembly YAML files that define how sections are compiled into editions",
  "type": "object",
  "required": ["id", "title", "include_status", "sections", "register"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique identifier for this assembly"
    },
    "title": {
      "type": "string",
      "description": "Human-readable title of the assembled edition"
    },
    "subtitle": {
      "type": "string",
      "description": "Optional subtitle shown on the PDF cover page"
    },
    "description": {
      "type": "string",
      "description": "What this assembly is for"
    },
    "include_status": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["draft", "candidate", "ratified", "deprecated"]
      },
      "description": "Only include sections with these statuses"
    },
    "register": {
      "type": "string",
      "enum": ["ritual", "spec", "both"],
      "description": "Which register(s) to include. 'both' includes Ritual then Spec per section."
    },
    "sections": {
      "type": "array",
      "items": {
        "oneOf": [
          { "type": "string" },
          {
            "type": "object",
            "required": ["path"],
            "additionalProperties": false,
            "properties": {
              "path": {
                "type": "string",
                "description": "Section file path relative to repo root"
              },
              "register": {
                "type": "string",
                "enum": ["ritual", "spec", "both"],
                "description": "Register override for this section"
              }
            }
          }
        ]
      },
      "description": "Ordered list of section file paths (strings) or objects with path and optional register override"
    },
    "pages": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Ordered list of pages to include in the document. Each entry is either a special keyword ('cover', 'toc', 'sections') or a markdown file path relative to repo root."
    },
    "output": {
      "type": "string",
      "description": "Optional output filename stem (without extension) to override the default derived from the assembly filename"
    },
    "margins": {
      "type": "object",
      "description": "Optional margin overrides for specific page types in the PDF output",
      "additionalProperties": false,
      "properties": {
        "spec": {
          "type": "string",
          "description": "CSS margin shorthand for spec pages (e.g. '0.6in 1.1in 0.5in 1.1in'). Default: '1in 1.1in 1.1in 1.1in'"
        }
      }
    },
    "auto_build": {
      "type": "boolean",
      "description": "If false, this assembly is skipped during 'make compose', 'make pdf --all', etc. Build it explicitly with 'make pdf ASSEMBLY=...' (default: true)"
    },
    "groups": {
      "type": "array",
      "description": "Optional groupings of sections into named songs or collections, with optional URLs",
      "items": {
        "type": "object",
        "required": ["title"],
        "additionalProperties": false,
        "properties": {
          "title": {
            "type": "string",
            "description": "Display title for the group or song"
          },
          "url": {
            "type": "string",
            "description": "URL to a playable or published version of this group"
          },
          "sections": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Section IDs belonging to this group"
          }
        }
      }
    }
  }
}
