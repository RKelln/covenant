/**
 * Terminal configuration types — re-exported from the main types module
 * for convenient import from the config package.
 */
export type { TerminalConfig, ProviderConfig, CouncilMemberConfig } from '../types'
