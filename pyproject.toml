[project]
name = "covenant"
version = "0.2.1"
description = "Build tools for the Covenant project"
requires-python = ">=3.11"
dependencies = [
    "jsonschema>=4.17",
    "markdown>=3.5",
    "pillow>=10.0",
    "pyyaml>=6.0",
    "weasyprint>=61.0",
]

